import React from 'react';

export const metadata = {
  title: 'Lombok Dev | Registrasi Lombok Dev Meetup 12',
};

export default function RootLayout({ children }) {
  return <main>{children}</main>;
}
